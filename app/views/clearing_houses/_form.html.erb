<%= form_for(@clearing_house) do |f| %>
  <% if @clearing_house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clearing_house.errors.count, "error") %> prohibited this clearing_house from being saved:</h2>

      <ul>
      <% @clearing_house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :publisher_id %><br>
    <%= f.number_field :publisher_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
